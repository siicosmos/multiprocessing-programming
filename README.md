# Simple Extensible Pipeline with Parallel Worker Pool and Queue (Python)

These examples are written in Python 3 with a different choice of Python module (multiprocessing vs asyncio) to achieve parallelism.
